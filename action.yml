name: 'GitDiff Docker push'
description: 'Check git diff & Push docker images'
inputs:
  github-token:
    required: false
  config-file-name:
    required: false
    default: 'dockermeta.yaml'
runs:
  using: 'node16'
  main: 'dist/index.js'
